"""Replay and discover: read an agent's local history, find how work actually flows, and rehearse rules against it.

Standard library only. Everything runs on this machine; nothing is sent anywhere unless the person asks for the
optional GitHub check, which uses their own `gh` login and only reads.
"""

__version__ = "0.2.0"
